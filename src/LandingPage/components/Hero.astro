<!-- Hero Component - Web3 Dark Theme with 3D Animation -->
<section class="hero-section">
  <!-- Animated Background -->
  <div class="bg-decoration">
    <!-- Animated Concentric Circles - Top Right (Pink/Magenta) -->
    <div class="circle-group top-right">
      <div class="circle circle-1"></div>
      <div class="circle circle-2"></div>
      <div class="circle circle-3"></div>
      <div class="circle circle-4"></div>
      <div class="circle circle-5"></div>
      <div class="circle circle-6"></div>
    </div>

    <!-- Animated Concentric Circles - Bottom Left (Blue/Cyan) -->
    <div class="circle-group bottom-left">
      <div class="circle circle-1"></div>
      <div class="circle circle-2"></div>
      <div class="circle circle-3"></div>
      <div class="circle circle-4"></div>
      <div class="circle circle-5"></div>
      <div class="circle circle-6"></div>
    </div>

    <!-- Glow Effects -->
    <div class="glow-orb orb-pink"></div>
    <div class="glow-orb orb-blue"></div>
    <div class="glow-orb orb-purple"></div>
  </div>

  <div class="hero-container">
    <!-- Content -->
    <div class="hero-content">
      <h1 class="hero-title">
        Discipline will take you places <span class="highlight">motivation can't</span>
      </h1>
      
      <p class="hero-description">
        Amet minim mollit non deserunt ullamco est sit aliqua dolor do amet sint. Velit officia consequat duis enim velit mollit. Exercitation veniam consequat sunt nostrud amet.
      </p>
      
      <div class="hero-buttons">
        <button class="btn btn-primary">
          <span>Schedule a Demo</span>
        </button>
        <button class="btn btn-secondary">
          <span>Contact Sales</span>
        </button>
      </div>
    </div>

    <!-- 3D Isometric Tablet Mockup -->
    <div class="hero-mockup">
      <div class="mockup-container">
        <!-- Isometric Tablet Device -->
        <div class="device-isometric">
          <!-- Outer Glow Layer -->
          <div class="device-outer-glow"></div>
          
          <!-- Device Frame/Border -->
          <div class="device-frame">
            <!-- Thick Glowing Border -->
            <div class="device-border-thick">
              <!-- Inner Border Glow -->
              <div class="border-inner-glow"></div>
            </div>
            
            <!-- Device Screen -->
            <div class="device-screen">
              <!-- Screen Background -->
              <div class="screen-bg"></div>
              
              <!-- Screen Content with Icon -->
              <div class="screen-content">
                <!-- Icon Circle Background -->
                <div class="icon-circle-bg">
                  <div class="icon-circle-glow"></div>
                </div>
                
                <!-- Icon Container -->
                <div class="icon-container">
                  <div class="icon-glow"></div>
                  <svg class="icon-svg" viewBox="0 0 32 32" fill="none">
                    <!-- Telegram/Send Icon -->
                    <path d="M16 4L4 11L16 18L28 11L16 4Z" fill="url(#blueGradient)" opacity="0.9"/>
                    <path d="M4 21L16 28L28 21" stroke="url(#blueGradient)" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
                    <path d="M4 16L16 23L28 16" stroke="url(#blueGradient)" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
                    <circle cx="16" cy="16" r="2" fill="#00D4FF" opacity="0.8"/>
                    <defs>
                      <linearGradient id="blueGradient" x1="4" y1="4" x2="28" y2="28">
                        <stop offset="0%" stop-color="#00D4FF"/>
                        <stop offset="50%" stop-color="#0099FF"/>
                        <stop offset="100%" stop-color="#0066FF"/>
                      </linearGradient>
                    </defs>
                  </svg>
                </div>
                
                <div class="screen-text">1280 × 945px</div>
              </div>
            </div>
          </div>
          
          <!-- Device Shadow -->
          <div class="device-shadow"></div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  .hero-section {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    background: linear-gradient(180deg, 
      #0a0118 0%,
      #150828 25%,
      #1a0d35 50%,
      #1f1142 75%,
      #24154f 100%
    );
  }

  /* Background Decorations */
  .bg-decoration {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
  }

  /* Animated Concentric Circles */
  .circle-group {
    position: absolute;
    width: 600px;
    height: 600px;
  }

  .circle-group.top-right {
    top: -200px;
    right: -100px;
  }

  .circle-group.bottom-left {
    bottom: -200px;
    left: -100px;
  }

  .circle {
    position: absolute;
    top: 50%;
    left: 50%;
    border-radius: 50%;
    border: 2px solid;
    transform: translate(-50%, -50%);
  }

  /* Top Right Circles - Pink/Magenta */
  .top-right .circle {
    border-color: rgba(236, 72, 153, 0.3);
    animation: ripple 8s ease-out infinite;
  }

  .top-right .circle-1 {
    width: 100px;
    height: 100px;
    animation-delay: 0s;
  }

  .top-right .circle-2 {
    width: 180px;
    height: 180px;
    animation-delay: 0.5s;
  }

  .top-right .circle-3 {
    width: 260px;
    height: 260px;
    animation-delay: 1s;
  }

  .top-right .circle-4 {
    width: 340px;
    height: 340px;
    animation-delay: 1.5s;
  }

  .top-right .circle-5 {
    width: 420px;
    height: 420px;
    animation-delay: 2s;
  }

  .top-right .circle-6 {
    width: 500px;
    height: 500px;
    animation-delay: 2.5s;
  }

  /* Bottom Left Circles - Blue/Cyan */
  .bottom-left .circle {
    border-color: rgba(59, 130, 246, 0.3);
    animation: ripple 8s ease-out infinite;
  }

  .bottom-left .circle-1 {
    width: 100px;
    height: 100px;
    animation-delay: 0.3s;
  }

  .bottom-left .circle-2 {
    width: 180px;
    height: 180px;
    animation-delay: 0.8s;
  }

  .bottom-left .circle-3 {
    width: 260px;
    height: 260px;
    animation-delay: 1.3s;
  }

  .bottom-left .circle-4 {
    width: 340px;
    height: 340px;
    animation-delay: 1.8s;
  }

  .bottom-left .circle-5 {
    width: 420px;
    height: 420px;
    animation-delay: 2.3s;
  }

  .bottom-left .circle-6 {
    width: 500px;
    height: 500px;
    animation-delay: 2.8s;
  }

  /* Glow Orbs */
  .glow-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(100px);
    opacity: 0.4;
    pointer-events: none;
  }

  .orb-pink {
    top: 5%;
    right: 10%;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(236, 72, 153, 0.6) 0%, transparent 70%);
    animation: pulse 8s ease-in-out infinite;
  }

  .orb-blue {
    bottom: 10%;
    left: 5%;
    width: 450px;
    height: 450px;
    background: radial-gradient(circle, rgba(59, 130, 246, 0.6) 0%, transparent 70%);
    animation: pulse 10s ease-in-out infinite 2s;
  }

  .orb-purple {
    top: 50%;
    left: 50%;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(147, 51, 234, 0.4) 0%, transparent 70%);
    animation: pulse 12s ease-in-out infinite 4s;
    transform: translate(-50%, -50%);
  }

  /* Container */
  .hero-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 3rem 4rem;
    display: grid;
    grid-template-columns: 1fr;
    gap: 4rem;
    align-items: center;
    position: relative;
    z-index: 1;
  }

  /* Content */
  .hero-content {
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
    animation: fadeInUp 0.8s ease-out;
  }

  .hero-title {
    font-size: 4rem;
    font-weight: 800;
    line-height: 1.15;
    margin-bottom: 1.5rem;
    color: #ffffff;
    letter-spacing: -0.02em;
  }

  .highlight {
    background: linear-gradient(135deg, #ec4899 0%, #8b5cf6 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-description {
    font-size: 1.1rem;
    line-height: 1.7;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 2.5rem;
    font-weight: 400;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .hero-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    padding: 0.9rem 2rem;
    border-radius: 8px;
    font-weight: 600;
    font-size: 0.95rem;
    border: none;
    cursor: pointer;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    position: relative;
    overflow: hidden;
  }

  .btn::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.1), transparent);
    transform: translateX(-100%);
    transition: transform 0.6s;
  }

  .btn:hover::before {
    transform: translateX(100%);
  }

  .btn-primary {
    background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
    color: white;
    box-shadow: 0 4px 20px rgba(59, 130, 246, 0.4);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 25px rgba(59, 130, 246, 0.5);
  }

  .btn-secondary {
    background: rgba(255, 255, 255, 0.1);
    color: white;
    border: 1px solid rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(10px);
  }

  .btn-secondary:hover {
    transform: translateY(-2px);
    background: rgba(255, 255, 255, 0.15);
    border-color: rgba(255, 255, 255, 0.3);
  }

  /* 3D Isometric Tablet Mockup */
  .hero-mockup {
    position: relative;
    height: 550px;
    perspective: 2000px;
    animation: fadeIn 1s ease-out 0.4s backwards;
    margin-top: 3rem;
  }

  .mockup-container {
    position: relative;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    transform-style: preserve-3d;
  }

  .device-isometric {
    position: relative;
    width: 700px;
    height: 450px;
    transform: rotateX(55deg) rotateZ(45deg);
    transform-style: preserve-3d;
    animation: float3d 6s ease-in-out infinite;
  }

  /* Outer Glow Layer */
  .device-outer-glow {
    position: absolute;
    inset: -30px;
    border-radius: 35px;
    background: linear-gradient(135deg, 
      rgba(59, 130, 246, 0.4) 0%,
      rgba(147, 51, 234, 0.3) 50%,
      rgba(236, 72, 153, 0.4) 100%
    );
    filter: blur(40px);
    opacity: 0.8;
    animation: outerGlowPulse 4s ease-in-out infinite;
    transform: translateZ(-5px);
  }

  /* Device Frame */
  .device-frame {
    position: absolute;
    inset: 0;
    border-radius: 24px;
    transform-style: preserve-3d;
  }

  /* Thick Glowing Border */
  .device-border-thick {
    position: absolute;
    inset: 0;
    border-radius: 24px;
    padding: 6px;
    background: linear-gradient(135deg, 
      rgba(59, 130, 246, 0.9) 0%,
      rgba(96, 165, 250, 0.8) 25%,
      rgba(147, 51, 234, 0.7) 50%,
      rgba(168, 85, 247, 0.8) 75%,
      rgba(59, 130, 246, 0.9) 100%
    );
    transform: translateZ(15px);
    box-shadow: 
      0 0 30px rgba(59, 130, 246, 0.6),
      0 0 60px rgba(147, 51, 234, 0.4),
      inset 0 0 20px rgba(59, 130, 246, 0.3);
    animation: borderPulse 3s ease-in-out infinite;
  }

  /* Inner Border Glow */
  .border-inner-glow {
    position: absolute;
    inset: -8px;
    border-radius: 28px;
    background: linear-gradient(135deg, 
      rgba(59, 130, 246, 0.6) 0%,
      rgba(147, 51, 234, 0.5) 50%,
      rgba(59, 130, 246, 0.6) 100%
    );
    filter: blur(25px);
    opacity: 0.9;
    animation: innerGlowPulse 3.5s ease-in-out infinite;
  }

  /* Device Screen */
  .device-screen {
    position: absolute;
    inset: 6px;
    border-radius: 20px;
    background: linear-gradient(135deg, 
      rgba(15, 23, 42, 0.98) 0%,
      rgba(20, 30, 50, 0.98) 50%,
      rgba(30, 41, 59, 0.98) 100%
    );
    backdrop-filter: blur(20px);
    overflow: hidden;
    transform: translateZ(10px);
    box-shadow: 
      inset 0 0 40px rgba(0, 0, 0, 0.5),
      inset 0 0 80px rgba(59, 130, 246, 0.1);
  }

  /* Screen Background */
  .screen-bg {
    position: absolute;
    inset: 0;
    background: 
      radial-gradient(circle at 30% 30%, rgba(59, 130, 246, 0.05) 0%, transparent 50%),
      radial-gradient(circle at 70% 70%, rgba(147, 51, 234, 0.05) 0%, transparent 50%);
  }

  .screen-content {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 2.5rem;
    position: relative;
    z-index: 1;
  }

  /* Icon Circle Background */
  .icon-circle-bg {
    position: absolute;
    width: 180px;
    height: 180px;
    border-radius: 50%;
    background: radial-gradient(circle, 
      rgba(59, 130, 246, 0.15) 0%,
      rgba(59, 130, 246, 0.05) 50%,
      transparent 70%
    );
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .icon-circle-glow {
    position: absolute;
    inset: -20px;
    border-radius: 50%;
    background: radial-gradient(circle, 
      rgba(59, 130, 246, 0.4) 0%,
      transparent 60%
    );
    filter: blur(40px);
    animation: iconPulse 2.5s ease-in-out infinite;
  }

  /* Icon Container */
  .icon-container {
    position: relative;
    width: 140px;
    height: 140px;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 2;
  }

  .icon-glow {
    position: absolute;
    inset: -30px;
    border-radius: 50%;
    background: radial-gradient(circle, 
      rgba(59, 130, 246, 0.8) 0%,
      rgba(59, 130, 246, 0.4) 40%,
      transparent 70%
    );
    filter: blur(35px);
    animation: iconPulse 2.5s ease-in-out infinite;
  }

  .icon-svg {
    width: 100px;
    height: 100px;
    position: relative;
    z-index: 1;
    animation: iconFloat 3s ease-in-out infinite;
    filter: drop-shadow(0 0 20px rgba(59, 130, 246, 0.6));
  }

  .screen-text {
    font-size: 1.1rem;
    color: rgba(255, 255, 255, 0.4);
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    z-index: 2;
  }

  /* Device Shadow */
  .device-shadow {
    position: absolute;
    bottom: -120px;
    left: 50%;
    transform: translateX(-50%) rotateX(90deg);
    width: 130%;
    height: 250px;
    background: radial-gradient(ellipse, 
      rgba(0, 0, 0, 0.7) 0%,
      rgba(0, 0, 0, 0.4) 40%,
      transparent 70%
    );
    filter: blur(50px);
    opacity: 0.8;
  }

  /* Animations */
  @keyframes ripple {
    0% {
      transform: translate(-50%, -50%) scale(0.8);
      opacity: 0;
    }
    50% {
      opacity: 0.5;
    }
    100% {
      transform: translate(-50%, -50%) scale(1.2);
      opacity: 0;
    }
  }

  @keyframes pulse {
    0%, 100% {
      opacity: 0.4;
      transform: scale(1);
    }
    50% {
      opacity: 0.6;
      transform: scale(1.05);
    }
  }

  @keyframes float3d {
    0%, 100% {
      transform: rotateX(55deg) rotateZ(45deg) translateY(0);
    }
    50% {
      transform: rotateX(55deg) rotateZ(45deg) translateY(-25px);
    }
  }

  @keyframes outerGlowPulse {
    0%, 100% {
      opacity: 0.8;
      transform: translateZ(-5px) scale(1);
    }
    50% {
      opacity: 1;
      transform: translateZ(-5px) scale(1.05);
    }
  }

  @keyframes innerGlowPulse {
    0%, 100% {
      opacity: 0.9;
    }
    50% {
      opacity: 1;
    }
  }

  @keyframes borderPulse {
    0%, 100% {
      box-shadow: 
        0 0 30px rgba(59, 130, 246, 0.6),
        0 0 60px rgba(147, 51, 234, 0.4),
        inset 0 0 20px rgba(59, 130, 246, 0.3);
    }
    50% {
      box-shadow: 
        0 0 40px rgba(59, 130, 246, 0.8),
        0 0 80px rgba(147, 51, 234, 0.6),
        inset 0 0 30px rgba(59, 130, 246, 0.5);
    }
  }

  @keyframes iconPulse {
    0%, 100% {
      opacity: 0.8;
      transform: scale(1);
    }
    50% {
      opacity: 1;
      transform: scale(1.15);
    }
  }

  @keyframes iconFloat {
    0%, 100% {
      transform: translateY(0) rotate(0deg);
    }
    50% {
      transform: translateY(-10px) rotate(5deg);
    }
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .hero-container {
      padding: 2rem;
    }

    .hero-title {
      font-size: 3rem;
    }

    .hero-description {
      font-size: 1rem;
    }

    .hero-mockup {
      height: 480px;
    }

    .device-isometric {
      width: 580px;
      height: 380px;
    }

    .icon-svg {
      width: 85px;
      height: 85px;
    }

    .circle-group {
      width: 500px;
      height: 500px;
    }
  }

  @media (max-width: 768px) {
    .hero-title {
      font-size: 2.5rem;
    }

    .hero-description {
      font-size: 0.95rem;
    }

    .hero-mockup {
      height: 420px;
    }

    .device-isometric {
      width: 480px;
      height: 320px;
    }

    .icon-svg {
      width: 70px;
      height: 70px;
    }

    .icon-container {
      width: 110px;
      height: 110px;
    }

    .btn {
      padding: 0.8rem 1.5rem;
      font-size: 0.9rem;
    }

    .circle-group {
      width: 400px;
      height: 400px;
    }
  }

  @media (max-width: 480px) {
    .hero-container {
      padding: 1.5rem;
    }

    .hero-title {
      font-size: 2rem;
    }

    .hero-description {
      font-size: 0.9rem;
    }

    .hero-buttons {
      flex-direction: column;
      width: 100%;
    }

    .btn {
      width: 100%;
      justify-content: center;
    }

    .hero-mockup {
      height: 320px;
    }

    .device-isometric {
      width: 350px;
      height: 230px;
    }

    .icon-svg {
      width: 55px;
      height: 55px;
    }

    .icon-container {
      width: 90px;
      height: 90px;
    }

    .screen-text {
      font-size: 0.85rem;
    }

    .circle-group {
      width: 300px;
      height: 300px;
    }
  }
</style>